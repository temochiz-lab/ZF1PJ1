# プログラムの流れ

対象アプリ: `zend-framework-1-crud-master`

## 最初に解析したファイル

```text
public/index.php
↓
application/configs/application.ini
↓
application/Bootstrap.php
↓
application/configs/routes.php
↓
application/controllers/TicketController.php
↓
application/models/TicketMapper.php
↓
application/models/DbTable/TicketDbTable.php
↓
application/models/Ticket.php
↓
application/views/scripts/ticket/*.phtml
↓
application/layouts/scripts/layout.phtml
```

```mermaid
flowchart TD
    F1["public/index.php<br>入口"]
    F2["application/configs/application.ini<br>設定"]
    F3["application/Bootstrap.php<br>初期化"]
    F4["application/configs/routes.php<br>URL定義"]
    F5["application/controllers/TicketController.php<br>Controller"]
    F6["application/models/TicketMapper.php<br>Mapper"]
    F7["application/models/DbTable/TicketDbTable.php<br>DbTable"]
    F8["application/models/Ticket.php<br>Model"]
    F9["application/views/scripts/ticket/*.phtml<br>View"]
    F10["application/layouts/scripts/layout.phtml<br>Layout"]

    F1 --> F2 --> F3 --> F4 --> F5 --> F6 --> F7 --> F8 --> F9 --> F10

    classDef entry fill:#dbeafe,stroke:#2563eb,color:#111827
    classDef config fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827

    class F1 entry
    class F2,F3,F4 config
    class F5 controller
    class F6,F7,F8 model
    class F9,F10 view
```

## 全体の入口

```text
ブラウザからアクセス
↓
public/index.php
  - APPLICATION_PATH を定義
  - APPLICATION_ENV を定義
  - library を include_path に追加
  - Zend/Application.php を読み込み
↓
Zend_Application を作成
↓
application/configs/application.ini を読み込み
↓
$application->bootstrap()->run()
```

```mermaid
flowchart TD
    Browser["ブラウザからアクセス"] --> Index["public/index.php"]
    Index --> DefinePath["APPLICATION_PATH<br>APPLICATION_ENV を定義"]
    DefinePath --> IncludePath["library を<br>include_path に追加"]
    IncludePath --> LoadZend["Zend/Application.php<br>読み込み"]
    LoadZend --> App["Zend_Application<br>作成"]
    App --> Ini["application.ini<br>読み込み"]
    Ini --> Run["bootstrap()->run()"]

    classDef user fill:#fef3c7,stroke:#d97706,color:#111827
    classDef entry fill:#dbeafe,stroke:#2563eb,color:#111827
    classDef config fill:#fff7ed,stroke:#ea580c,color:#111827
    classDef framework fill:#ede9fe,stroke:#7c3aed,color:#111827

    class Browser user
    class Index entry
    class DefinePath,IncludePath,Ini config
    class LoadZend,App,Run framework
```

## Bootstrapの流れ

```text
application/Bootstrap.php
↓
_initDoctype()
  - Viewを初期化
  - doctypeを XHTML1_STRICT に設定
↓
_initRoutes()
  - FrontControllerのRouterを取得
  - application/configs/routes.php を読み込み
↓
_initUploadDirAndConstant()
  - application/uploads がなければ作成
  - UPLOAD_PATH 定数を定義
```

```mermaid
flowchart TD
    Bootstrap["application/Bootstrap.php"]
    Bootstrap --> Doctype["_initDoctype()"]
    Doctype --> ViewInit["Viewを初期化"]
    Doctype --> Xhtml["doctypeを<br>XHTML1_STRICT に設定"]

    Bootstrap --> Routes["_initRoutes()"]
    Routes --> Router["FrontControllerの<br>Routerを取得"]
    Routes --> RoutesFile["routes.php を読み込み"]

    Bootstrap --> Upload["_initUploadDirAndConstant()"]
    Upload --> MakeDir["application/uploads<br>がなければ作成"]
    Upload --> Const["UPLOAD_PATH<br>定数を定義"]

    classDef bootstrap fill:#ede9fe,stroke:#7c3aed,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827
    classDef route fill:#e0f2fe,stroke:#0284c7,color:#111827
    classDef file fill:#fef3c7,stroke:#d97706,color:#111827

    class Bootstrap,Doctype,Routes,Upload bootstrap
    class ViewInit,Xhtml view
    class Router,RoutesFile route
    class MakeDir,Const file
```

## ルーティングの流れ

```text
application/configs/routes.php
↓
/ 
  → TicketController#indexAction
↓
/ticket/create
  → TicketController#saveAction
↓
/ticket/edit/:id
  → TicketController#editAction
↓
/ticket/delete/:id
  → TicketController#deleteAction
↓
/ticket/cvsexport
  → TicketController#cvsexportAction
```

```mermaid
flowchart LR
    Routes["routes.php"]
    Routes --> Top["/<br>indexAction"]
    Routes --> Create["/ticket/create<br>saveAction"]
    Routes --> Edit["/ticket/edit/:id<br>editAction"]
    Routes --> Delete["/ticket/delete/:id<br>deleteAction"]
    Routes --> Csv["/ticket/cvsexport<br>cvsexportAction"]

    Top --> Controller["TicketController"]
    Create --> Controller
    Edit --> Controller
    Delete --> Controller
    Csv --> Controller

    classDef route fill:#e0f2fe,stroke:#0284c7,color:#111827
    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827

    class Routes route
    class Top,Create,Edit,Delete,Csv url
    class Controller controller
```

## 一覧表示の流れ

```text
GET /
↓
routes.php
↓
TicketController#indexAction
↓
Application_Model_TicketMapper を作成
↓
TicketMapper#fetchAllTopics()
↓
TicketDbTable
↓
DBテーブル tickets から id DESC で取得
↓
取得した行を Application_Model_Ticket に詰め替え
↓
Zend_Paginator でページング
↓
view に blogTopics を渡す
↓
application/views/scripts/ticket/index.phtml
↓
application/views/scripts/controls.phtml
↓
application/layouts/scripts/layout.phtml
↓
チケット一覧画面を表示
```

```mermaid
flowchart TD
    Req["GET /"] --> Routes["routes.php"]
    Routes --> Action["TicketController#indexAction"]
    Action --> Mapper["TicketMapper<br>fetchAllTopics()"]
    Mapper --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>id DESCで取得")]
    DB --> Model["Application_Model_Ticket<br>に詰め替え"]
    Model --> Paginator["Zend_Paginator<br>5件ずつページング"]
    Paginator --> ViewData["viewへ<br>blogTopics を渡す"]
    ViewData --> IndexView["ticket/index.phtml"]
    IndexView --> Controls["controls.phtml"]
    Controls --> Layout["layout.phtml"]
    Layout --> Screen["チケット一覧画面"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827
    classDef framework fill:#ede9fe,stroke:#7c3aed,color:#111827

    class Req,Routes url
    class Action controller
    class Mapper,DbTable,Model model
    class DB db
    class Paginator framework
    class ViewData,IndexView,Controls,Layout,Screen view
```

## 新規作成の流れ

```text
GET /ticket/create
↓
routes.php
↓
TicketController#saveAction
↓
Application_Form_TopicBootstrapForm を作成
↓
application/views/scripts/ticket/save.phtml
↓
チケット作成フォームを表示
```

```mermaid
flowchart TD
    Req["GET /ticket/create"] --> Routes["routes.php"]
    Routes --> Action["TicketController#saveAction"]
    Action --> Form["TopicBootstrapForm<br>を作成"]
    Form --> View["ticket/save.phtml"]
    View --> Screen["チケット作成フォームを表示"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef form fill:#ede9fe,stroke:#7c3aed,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827

    class Req,Routes url
    class Action controller
    class Form form
    class View,Screen view
```

```text
POST /ticket/create
↓
TicketController#saveAction
↓
フォーム入力を検証
↓
Application_Model_Ticket にフォーム値を詰める
↓
アップロードファイルを受け取る
↓
TicketMapper#saveTopic()
↓
id がないため insert
↓
TicketDbTable
↓
DBテーブル tickets に登録
↓
FlashMessenger に成功/失敗メッセージを追加
↓
/ にリダイレクト
```

```mermaid
flowchart TD
    Req["POST /ticket/create"] --> Action["TicketController#saveAction"]
    Action --> Validate["フォーム入力を検証"]
    Validate --> Ticket["Application_Model_Ticket<br>にフォーム値を詰める"]
    Ticket --> Upload["アップロードファイルを受け取る"]
    Upload --> Mapper["TicketMapper#saveTopic()"]
    Mapper --> Insert["idなし<br>insert"]
    Insert --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>登録")]
    DB --> Flash["FlashMessenger<br>成功/失敗メッセージ"]
    Flash --> Redirect["/ にリダイレクト"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef form fill:#ede9fe,stroke:#7c3aed,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef helper fill:#e0f2fe,stroke:#0284c7,color:#111827

    class Req,Redirect url
    class Action controller
    class Validate,Upload form
    class Ticket,Mapper,Insert,DbTable model
    class DB db
    class Flash helper
```

## 編集の流れ

```text
GET /ticket/edit/:id
↓
routes.php
↓
TicketController#editAction
↓
Application_Form_TopicBootstrapForm を作成
↓
TicketMapper#findTopic(id)
↓
TicketDbTable
↓
DBテーブル tickets から対象idを取得
↓
フォームに既存データを populate
↓
application/views/scripts/ticket/edit.phtml
↓
編集フォームを表示
```

```mermaid
flowchart TD
    Req["GET /ticket/edit/:id"] --> Routes["routes.php"]
    Routes --> Action["TicketController#editAction"]
    Action --> Form["TopicBootstrapForm<br>を作成"]
    Action --> Mapper["TicketMapper#findTopic(id)"]
    Mapper --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>対象idを取得")]
    DB --> Populate["フォームに既存データを<br>populate"]
    Populate --> View["ticket/edit.phtml"]
    View --> Screen["編集フォームを表示"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef form fill:#ede9fe,stroke:#7c3aed,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827

    class Req,Routes url
    class Action controller
    class Form,Populate form
    class Mapper,DbTable model
    class DB db
    class View,Screen view
```

```text
POST /ticket/create
↓
TicketController#saveAction
↓
hidden の id を取得
↓
Application_Model_Ticket にフォーム値を詰める
↓
TicketMapper#saveTopic()
↓
id があるため update
↓
TicketDbTable
↓
DBテーブル tickets を更新
↓
/ にリダイレクト
```

```mermaid
flowchart TD
    Req["POST /ticket/create"] --> Action["TicketController#saveAction"]
    Action --> Hidden["hidden の id を取得"]
    Hidden --> Ticket["Application_Model_Ticket<br>にフォーム値を詰める"]
    Ticket --> Mapper["TicketMapper#saveTopic()"]
    Mapper --> Update["idあり<br>update"]
    Update --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>更新")]
    DB --> Redirect["/ にリダイレクト"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef form fill:#ede9fe,stroke:#7c3aed,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827

    class Req,Redirect url
    class Action controller
    class Hidden form
    class Ticket,Mapper,Update,DbTable model
    class DB db
```

## 削除の流れ

```text
GET /ticket/delete/:id
↓
routes.php
↓
TicketController#deleteAction
↓
リクエストから id を取得
↓
TicketMapper#deleteTopic(id)
↓
TicketDbTable
↓
DBテーブル tickets から対象idを削除
↓
FlashMessenger に成功/失敗メッセージを追加
↓
/ にリダイレクト
```

```mermaid
flowchart TD
    Req["GET /ticket/delete/:id"] --> Routes["routes.php"]
    Routes --> Action["TicketController#deleteAction"]
    Action --> Id["リクエストから<br>idを取得"]
    Id --> Mapper["TicketMapper#deleteTopic(id)"]
    Mapper --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>対象idを削除")]
    DB --> Flash["FlashMessenger<br>成功/失敗メッセージ"]
    Flash --> Redirect["/ にリダイレクト"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef helper fill:#e0f2fe,stroke:#0284c7,color:#111827

    class Req,Routes,Redirect url
    class Action controller
    class Id,Mapper,DbTable model
    class DB db
    class Flash helper
```

## CSV出力の流れ

```text
GET /ticket/cvsexport
↓
routes.php
↓
TicketController#cvsexportAction
↓
Application_Model_TicketMapper を作成
↓
TicketMapper#fetchAllCvs()
↓
TicketDbTable
↓
DBテーブル tickets から id DESC で取得
↓
配列形式に変換
↓
viewRenderer->setNoRender()
↓
Csv helper を呼び出す
↓
tickets.csv 相当のCSVを出力
```

```mermaid
flowchart TD
    Req["GET /ticket/cvsexport"] --> Routes["routes.php"]
    Routes --> Action["TicketController#cvsexportAction"]
    Action --> Mapper["TicketMapper<br>fetchAllCvs()"]
    Mapper --> DbTable["TicketDbTable"]
    DbTable --> DB[("tickets<br>id DESCで取得")]
    DB --> Array["配列形式に変換"]
    Array --> NoRender["viewRenderer<br>setNoRender()"]
    NoRender --> Csv["Csv helper"]
    Csv --> File["CSVを出力"]

    classDef url fill:#fef3c7,stroke:#d97706,color:#111827
    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef helper fill:#e0f2fe,stroke:#0284c7,color:#111827

    class Req,Routes url
    class Action controller
    class Mapper,DbTable,Array model
    class DB db
    class NoRender,Csv,File helper
```

## MVCごとの役割

```text
Controller
  TicketController.php
  - URLごとの処理を受け持つ
  - Modelを呼び出す
  - Viewへ表示データを渡す
  - 登録/更新/削除後にリダイレクトする

Model
  Ticket.php
  - チケット1件分のデータ入れ物

  TicketMapper.php
  - ControllerとDBテーブルの間をつなぐ
  - fetchAllTopics / findTopic / saveTopic / deleteTopic / fetchAllCvs を持つ

  DbTable/TicketDbTable.php
  - Zend_Db_Table_Abstract
  - DBテーブル tickets に対応する

View
  views/scripts/ticket/index.phtml
  views/scripts/ticket/save.phtml
  views/scripts/ticket/edit.phtml
  views/scripts/controls.phtml
  - 画面HTMLを組み立てる

Layout
  layouts/scripts/layout.phtml
  - 共通レイアウト
  - ナビゲーション
  - CSS/JS読み込み
  - FlashMessenger表示
```

```mermaid
flowchart LR
    Controller["Controller<br>TicketController.php"]
    Mapper["Model/Mapper<br>TicketMapper.php"]
    Entity["Model<br>Ticket.php"]
    DbTable["DbTable<br>TicketDbTable.php"]
    DB[("DB<br>tickets")]
    View["View<br>ticket/*.phtml"]
    Layout["Layout<br>layout.phtml"]

    Controller --> Mapper
    Mapper --> Entity
    Mapper --> DbTable
    DbTable --> DB
    Controller --> View
    View --> Layout

    classDef controller fill:#fee2e2,stroke:#dc2626,color:#111827
    classDef model fill:#dcfce7,stroke:#16a34a,color:#111827
    classDef db fill:#ffedd5,stroke:#ea580c,color:#111827
    classDef view fill:#fce7f3,stroke:#db2777,color:#111827

    class Controller controller
    class Mapper,Entity,DbTable model
    class DB db
    class View,Layout view
```

## 重要ポイント

- アプリの入口は `public/index.php`。
- URLの割り当ては `application/configs/routes.php`。
- トップページ `/` は `IndexController` ではなく `TicketController#indexAction` に流れる。
- 登録と更新はどちらも `TicketController#saveAction` が担当する。
- 編集画面のPOST先は `/ticket/create`。hidden項目の `id` の有無で insert/update が分かれる。
- DBアクセスは `TicketMapper.php` から `TicketDbTable.php` を通して行われる。
- 実テーブル名は `tickets`。
